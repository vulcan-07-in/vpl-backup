VARCHASVA PREMIER LEAGUE - DEEP-DIVE CODE AUDIT REPORT
======================================================
Focus: `scorer-client.tsx`, `tournament.ts`, and State Management.

### 1. Data Flow & Logic Audit (Source of truth)
✅ The Counting Logic (`applyBallEvent` and `rebuildState`) is mathematically sound. 
- Wides (`WD`) correctly grant 1 extra + runs completed without inflating batter balls faced.
- No Balls (`NB`) correctly assign bat runs to the striker, +1 to the team extras, and ignore ball count.
- Strike rotation logic for over-completion and odd-runs is perfectly separated.
✅ The Source of Truth (Dropdowns & Lists) in `LIVE_SCORING`, `TOSS_SETUP`, and `WICKET_MODAL` perfectly bound player selections using `getSquadForTeam` based on `currentInningsData.teamName` vs `fieldingInningsData.teamName`.
- `isOut` and active striker/non-striker filtering prevents duplicate player selection.

### 2. UI/UX & Interactive Flow (Critical UX Fixes)
❌ **Dead End Found (Match Selection):** 
When a scorer is on the `SELECT_MATCH` screen, they are completely trapped. There is no top bar and no `Logout` button visible. If a scorer accidentally logs in to the wrong device, they must forcefully clear browser cache or enter a random match just to access the Action menu to logout.
❌ **Mobile Sizing Constraints:** 
The massive iPad/Desktop control grid `col-span-6` translates well manually, but the `EDIT_OVERRIDE` modal has buttons pushed all the way to the bottom.

### 3. AI-Specific Bug Hunting
✅ No `useEffect` infinite loops detected.
- The `[liveState]` dependency array for syncing `openStriker`/`openBowler` on `LIVE` is redundant but safe since React bails out of identical pointer updates.
- "Ghost Props": `notifyMatch` and `handleSetActiveMatch` were detected as orphaned code fragments (commented out as `dead no-op`). Background syncing correctly replaced them with local storage queued APIs!

---

### ACTION TAKEN:
I have automatically updated `scorer-client.tsx` to fix the **Critical UI Dead End**. A dedicated `Logout` button has been injected into the `AVAILABLE FIXTURES` header on the match selection screen.
